= form_for [:setting, resource], :html => { :class => 'form-horizontal' } do |f|

  = render 'form_basic_fields', :f => f

  = render 'form_user_group', :f => f

  .form-actions
    = f.submit nil, :class => 'btn btn-primary'
    = cancel_resource_path